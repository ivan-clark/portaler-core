/**
 * Generates a random alphanumeric key
 * @param  size? If no size is defined, it randomly generates a key from 5 to 10 characters
 * @returns string
 */
export declare const keyGenerator: (size?: number | undefined) => string;
